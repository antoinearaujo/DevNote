{% extends "::base.html.twig" %}

{% block title %}DevNote Add{% endblock %}

{% block body %}

    {% if is_granted('ROLE_SUPER_ADMIN') %}

    <html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <style>
            /* Set height of the grid so .sidenav can be 100% (adjust if needed) */
            .row.content {height: 1500px}

            /* Set gray background color and 100% height */
            .sidenav {
                background-color: #f1f1f1;
                height: 100%;
            }

            /* Set black background color, white text and some padding */
            footer {
                background-color: #555;
                color: white;
                padding: 15px;
            }

            /* On small screens, set height to 'auto' for sidenav and grid */
            @media screen and (max-width: 767px) {
                .sidenav {
                    height: auto;
                    padding: 15px;
                }
                .row.content {height: auto;}
            }
        </style>
    </head>
    <body>

    <div class="container-fluid">
        <div class="row content">
            <div class="col-sm-3 sidenav">
                <h4>DevNote Panel</h4>
                <ul class="nav nav-pills nav-stacked">
                    <li><a href="{{ path('home_notes') }}">Notes</a></li>
                    <li class="active"><a href="{{ path('admin_article') }}">Ajouter une note</a></li>
                    <li><a href="{{ path('fos_user_security_logout') }}">Deconnexion</a></li>
                </ul><br>

            </div>
            <div class="row">
                <section class="col-sm-1">


                </section>
                <section class="col-sm-1">





                    <div class="row">
                        <section class="col-sm-4">


                        </section>
                        <section class="col-sm-1">




                        <div class="row">
            <section class="col-sm-2">

                {{ form_start(form) }}
                {{ form_widget(form.Name, { 'attr': { 'size' : 40 } }) }}

                {{ form_rest(form, { 'attr': { 'size' : 40 } }) }}


            </section>
        </div>



    </div>
    </div>

    <footer class="container-fluid">
        <p>DevNote 2017</p>
    </footer>

    </body>
    </html>




    {% endif %}

    {% if not is_granted ('ROLE_SUPER_ADMIN') %}

        <h1>Vous n'êtes pas administrateur</h1>

    {% endif %}
{% endblock %}